import { View, Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const PostInfo = () => {
  return (
    <SafeAreaView>
      <Text style={{ fontSize: 20, fontFamily: 'ComicSans' }}>info </Text>
    </SafeAreaView>
  )
}

export default PostInfo
