import { View, Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const Photo = () => {
  return (
    <SafeAreaView>
      <Text style={{ fontSize: 20, fontFamily: 'ComicSans' }}>photo</Text>
    </SafeAreaView>
  )
}

export default Photo
